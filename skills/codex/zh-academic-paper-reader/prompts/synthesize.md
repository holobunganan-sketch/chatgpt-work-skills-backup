# 全文综合任务

完成正文后重新通读所有认知单元，生成通用七要素与固定 synthesis。综合必须从正文证据出发，区分直接观察/数据、作者解释、推论和伴读解释。

`synthesis` 固定字段：`topic`、`problem`、`core_answer`、`how_answered`、`key_evidence`、`caution`、`contribution`、`unresolved`、`logic_chain`。

Protocol 的 `key_evidence` 只能描述计划、既有依据或设计逻辑，不能生成尚不存在的结果。
